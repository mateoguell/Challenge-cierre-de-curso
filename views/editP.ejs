<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <div>
        <h1>Editar peliculas</h1>
    </div>
    <form action="/peliculasD/editP/<%= movie.id %>" method="POST">
        <div>
            <input id="title" name="title" value="<%= movie.title %>" type="text" placeholder="Titulo">
        </div>

        <div>
            <input id="rating" name="rating" value="<%= movie.rating %>" type="number" placeholder="Puntaje">
        </div>
        <div>
            <input id="awards" name="awards" value="<%= movie.awards %>" type="number" placeholder="Premios">
        </div>
        <div>
            <input id="length" name="length" value="<%= movie.length %>" type="number" placeholder="Duración">
        </div>
        <div>
            <input id="release_date" name="release_date" value="<%= movie.release_date %>" type="date"
                placeholder="Fecha de Estreno">
        </div>
        <label for="Genre"></label>
        <select name="genre_id">
            <% for (let i=0; i < genres.length; i++) { %>
                <option value="<%= genres[i].id %>">
                    <%= genres[i].name %>
                </option>
                <% } %>
        </select>
        <div>
            <input type="submit" value="Editar">
        </div>
    </form>
    <div>
        <button><a href="/peliculasD/<%= movie.id %>">Volver</a></button>
    </div>
</body>

</html>